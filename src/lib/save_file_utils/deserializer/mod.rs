use super::Save;

#[allow(dead_code, unused_variables)]
/// read the bytes of the file at sav_file_path, and break down the data into a struct for use by other parts of the program
pub fn deserialize(sav_file_path: &std::path::PathBuf) -> Save {
    todo!();
}


#[cfg(test)]
mod tests {
    //unit tests
}
