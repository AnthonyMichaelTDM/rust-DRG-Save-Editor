use super::Save;

#[allow(dead_code, unused_variables)]
/// unpack the Save object into a .sav file.
pub fn serialize(save: &Save, sav_file_path: &std::path::PathBuf) {
    todo!();
}


#[cfg(test)]
mod tests {
    //unit tests
}
